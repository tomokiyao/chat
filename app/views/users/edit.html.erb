	<header class="navbar navbar-inverse">
		<div class="containar">
			<div class="row">
				<div class="col-xs-10 col-xs-offset-1">
					<nav class="nav navbar-nav navbar-left">
						<span>Bookers</span>
					</nav>
					<nav>
						<ul class="nav navbar-nav navbar-right">
							<li>
								<%=link_to " Home", user_path(current_user), class: "glyphicon glyphicon-home" %>
							</li>
							<li>
								<%=link_to " Users", users_path(@users), class: "glyphicon glyphicon-user" %>
							</li>
							<li>
								<%=link_to " Books", post_books_path(@post_books), class: "glyphicon glyphicon-book" %>
							</li>
							<li>
								<%=link_to " Logout", destroy_user_session_path, method: :delete, class: "glyphicon glyphicon-log-out" %>
							</li>
						</ul>
					</nav>
				</div>
			</div>
		</div>
	</header>


<div class="body">
	<div class="containar">
		<div class="row">
			<div class="col-xs-3">
				<h1>User info</h1>
				<%=form_for(@user) do |f| %>
				<div class="field">
					<label for="user_name">name</label>
					<%=f.text_field :name%>
				</div>
				<div class="field">
					<label for="user_image">Image</label>
						<%=f.attachment_field :profile_image, plaeholder:"プロフィール画像", class: "user-edit-form" %>
					</div>
				<div class="field">
					<label for="introduction">introduction</label>
					<%=f.text_field :introduction %>
				</div>
				<div class="action">
					<%=f.submit "Update User" %>
				</div>
					<% end %>
				</div>
				</div>
			</div>
		</div>
	</div>
</div>
